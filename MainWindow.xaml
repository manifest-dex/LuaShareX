<ui:FluentWindow x:Class="LuaShareX.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:LuaShareX.ViewModels"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="LuaShareX" Width="800" Height="600"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        TextElement.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <ui:TitleBar Grid.Row="0" Title="LuaShareX" ShowMaximize="False" />

        <!-- Content -->
        <ContentControl Grid.Row="1" Content="{Binding CurrentView}" Margin="16,8">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type vm:GamesViewModel}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock Text="LuaShareX" FontSize="22" FontWeight="Bold" Margin="0,0,0,2" />
                        <TextBlock Text="Export installed Steam games as Lua files" FontSize="12" Foreground="#6b7280" Margin="0,0,0,12" />

                        <!-- Manual Add -->
                        <Grid Grid.Row="1" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ui:TextBox Text="{Binding ManualAppId, Mode=TwoWay}" PlaceholderText="Add by App ID or game name"
                                      KeyDown="ManualAppId_KeyDown" Margin="0,0,8,0" />
                            <ui:Button Grid.Column="1" Content="Add" Command="{Binding AddManualGameCommand}" Padding="12,6" />
                        </Grid>

                        <!-- Filter + Actions -->
                        <Grid Grid.Row="2" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ui:TextBox Text="{Binding SearchText, Mode=TwoWay}" PlaceholderText="Filter games..." Margin="0,0,8,0" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <ui:Button Content="Refresh" Command="{Binding RefreshGamesCommand}" Margin="0,0,4,0" Padding="12,6" />
                                <ui:Button Content="Select All" Command="{Binding ToggleSelectAllCommand}" Margin="0,0,4,0" Padding="12,6" />
                                <ui:Button Content="Export .lua" Command="{Binding ExportSelectedCommand}" Padding="12,6" Appearance="Primary" />
                            </StackPanel>
                        </Grid>

                        <!-- Selected count -->
                        <Grid Grid.Row="3" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock VerticalAlignment="Center" FontSize="12" Foreground="#6b7280">
                                <Run Text="{Binding SelectedCount, Mode=OneWay}" />
                                <Run Text=" selected" />
                            </TextBlock>
                            <ui:Button Grid.Column="1" Content="Copy" Command="{Binding CopyToClipboardCommand}" Padding="12,6" />
                        </Grid>

                        <!-- Loading -->
                        <StackPanel Grid.Row="4" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <ui:ProgressRing IsIndeterminate="True" Width="32" Height="32" Margin="0,0,0,8" />
                            <TextBlock Text="{Binding StatusMessage}" FontSize="12" Foreground="#6b7280" HorizontalAlignment="Center" />
                        </StackPanel>

                        <!-- Games List -->
                        <ListView Grid.Row="4" ItemsSource="{Binding Games}" VirtualizingPanel.IsVirtualizing="True"
                                  VirtualizingPanel.VirtualizationMode="Recycling"
                                  BorderThickness="0" Background="Transparent"
                                  SelectionChanged="GamesList_SelectionChanged">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="0,2">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding AppId}" Foreground="#6b7280" FontSize="12" Margin="0,0,8,0" Width="70" />
                                            <TextBlock Text="{Binding Name}" FontSize="13" />
                                        </StackPanel>
                                    </CheckBox>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#08ffffff" BorderBrush="#12ffffff" BorderThickness="0,1,0,0" Padding="16,8">
            <TextBlock Text="{Binding StatusMessage}" FontSize="12" Foreground="#6b7280" VerticalAlignment="Center" />
        </Border>
    </Grid>
</ui:FluentWindow>
